

This project implements a complete testbench for verifying a Single Port RAM. It includes constrained-random stimulus generation, assertions, functional coverage, and a scoreboard to validate memory operations and protocol correctness. The RAM is 8 bits wide, 32 locations deep, and supports independent read or write operations. 
